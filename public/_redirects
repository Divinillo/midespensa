# MiDespensa – Cloudflare Pages redirects
# / → landing page (rewrite)
# /app → React SPA (served as physical file dist/app/index.html via build script)
# /app/* → handles any deep links within the SPA

/           /landing.html   200
/app/*      /app/index.html 200
