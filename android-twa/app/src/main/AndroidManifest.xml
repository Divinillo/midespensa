<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:supportsRtl="false"
        android:theme="@android:style/Theme.NoTitleBar">

        <!-- ── Actividad principal TWA ── -->
        <activity
            android:name="com.google.androidbrowserhelper.trusted.LauncherActivity"
            android:exported="true">

            <!-- URL de inicio de la PWA. El parámetro ?source=twa lo lee
                 utils/isAndroidTwa.ts en el cliente para activar la rama
                 de Google Play Billing en el modal de upgrade. -->
            <meta-data
                android:name="android.support.customtabs.trusted.DEFAULT_URL"
                android:value="https://midespensa.app/app?source=twa" />

            <!-- Color de la barra de estado durante carga -->
            <meta-data
                android:name="android.support.customtabs.trusted.STATUS_BAR_COLOR"
                android:value="#134e4a" />

            <!-- Color de la barra de navegación -->
            <meta-data
                android:name="android.support.customtabs.trusted.NAVIGATION_BAR_COLOR"
                android:value="#134e4a" />

            <!-- Color de fondo de la splash screen -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_BACKGROUND_COLOR"
                android:value="#0f766e" />

            <!-- Pantalla inicial mientras carga -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_IMAGE_DRAWABLE"
                android:resource="@drawable/splash_icon" />

            <!-- Versión del splash: mantener en 1 -->
            <meta-data
                android:name="android.support.customtabs.trusted.SPLASH_SCREEN_VERSION"
                android:value="1" />

            <!-- Fallback: abrir en Chrome si no hay Custom Tab -->
            <meta-data
                android:name="android.support.customtabs.trusted.FALLBACK_STRATEGY"
                android:value="customtabs" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Manejo de deeplinks para midespensa.app -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="midespensa.app" />
            </intent-filter>
        </activity>

        <!-- Servicio para actualizaciones en segundo plano de Custom Tabs -->
        <service
            android:name="com.google.androidbrowserhelper.trusted.DelegationService"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.support.customtabs.trusted.TRUSTED_WEB_ACTIVITY_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <!-- ── Google Play Billing bridge ──────────────────────────
             These two components come from the
             `com.google.androidbrowserhelper:billing` library and
             expose the Digital Goods API + PaymentRequest API to the
             web content running inside the TWA. Without them
             window.getDigitalGoodsService('https://play.google.com/billing')
             returns undefined and the in-app subscription flow fails. -->
        <activity
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="org.chromium.intent.action.PAY" />
            </intent-filter>
            <meta-data
                android:name="org.chromium.default_payment_method_name"
                android:value="https://play.google.com/billing" />
        </activity>

        <service
            android:name="com.google.androidbrowserhelper.playbilling.provider.PaymentService"
            android:exported="true">
            <intent-filter>
                <action android:name="org.chromium.intent.action.IS_READY_TO_PAY" />
            </intent-filter>
        </service>

    </application>
</manifest>
