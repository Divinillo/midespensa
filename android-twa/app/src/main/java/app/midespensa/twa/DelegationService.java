package app.midespensa.twa;

/**
 * Placeholder package-info class. The Trusted Web Activity is launched via
 * com.google.androidbrowserhelper.trusted.LauncherActivity declared in the
 * AndroidManifest, and Google Play Billing is wired in directly through the
 * PaymentActivity / PaymentService components from the
 * `com.google.androidbrowserhelper:billing` library — see AndroidManifest.xml.
 *
 * Kept here so the `app.midespensa.twa` Java source root exists; remove
 * once a real Java/Kotlin source file is added.
 */
final class DelegationService {
    private DelegationService() {}
}
